#include <stdio.h>
#include <stdlib.h>

int main()
{
    // integers
    int age = 40;

    // decimal numb
    double gpa = 3.7;
    float collegeGpa = 3.0;

    // characters, use single quotes. can only store a single character in the single quotes.
    char grade = 'A';
    
    // modification of the character dataType
    // string is a collection of the character dataType
    // create a string of characters using an array
    // special type of data to C
    char phrase[] = "Welcome Back!";
    
    return 0;
}